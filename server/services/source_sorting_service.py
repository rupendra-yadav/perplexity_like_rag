from typing import List


class SourceSorting:

#     def __init__(self):
#         self.embedding_model = SentenceTransformer("all-MiniLM-L6-v2")

    def sort_source(self,query:str,source_results:List[dict]):
        result = []
#         for one in source_results:
#            data= self.embedding_model.encode(one)
#            result.append(data)
        return result

